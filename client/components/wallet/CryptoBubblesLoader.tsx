import type { FC } from "react";

interface CryptoBubblesLoaderProps {
  className?: string;
  size?: number;
  width?: number;
  height?: number;
}

export const CryptoBubblesLoader: FC<CryptoBubblesLoaderProps> = () => null;
