[build]
command = "pnpm build"
publish = "dist"
functions = "netlify/functions"

# Timeout for long-running functions (in seconds)
[functions]
  timeout = 30

# SPA fallback for client-side routing (must be last)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
